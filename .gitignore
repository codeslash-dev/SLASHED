node_modules/
test-results/
playwright-report/
*.tgz
.DS_Store
.agents/
.claude/*.lock
.claude/todos/
.claude/cache/

# Semantic review artifacts (sub-agent output, not source)
semantic-review/

# Build artifacts in badges/ — generated by `npm run build`, served via the
# `dist` branch (version-sync.yml) and GitHub Releases. Not tracked in main
# to avoid binary conflicts and noise in PR diffs.
badges/slashed.zip
badges/*.css
badges/*.map

# Legacy build output dir (build script still emits here; real artifacts live in badges/)
dist/

# full-api demo validator scratch
demos/.validate/
