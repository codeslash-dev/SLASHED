node_modules/
test-results/
playwright-report/
*.tgz
.DS_Store
.agents/
.claude/*.lock
.claude/todos/
.claude/cache/

# Semantic review artifacts (sub-agent output, not source)
semantic-review/

# Build artifacts in dist/ — generated by `npm run build`, served via the
# `dist` branch (publish-dist.yml) and GitHub Releases. Not tracked in main
# to avoid binary conflicts and noise in PR diffs. dist/css-custom-data.json
# is the one tracked exception (checked in for editor tooling / npm publish).
dist/slashed.zip
dist/*.css
dist/*.map

# full-api demo validator scratch
demos/.validate/
demos/.audit/
